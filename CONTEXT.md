# RemoteHub

内网远程连接凭据管理系统：项目分组的连接凭据（RDP/SSH/VNC/VPN 等）存储、按需解密与团队协作。

## Language

### 在线与活动

**在线（Online）**:
`lastActiveAt` 距当前 5 分钟内的用户——产品唯一在线口径，前端顶栏与 admin 仪表盘同源同数。
_Avoid_: 活跃会话数、Session 在线（未过期会话 ≠ 在线）

**活动流（Activity Feed）**:
仪表盘「最近活动」列表——含成功、失败与安全事件的**全量**审计记录流。
_Avoid_: 最近操作（无成败维度暗示）

**活跃趋势（Activity Trend）**:
每日登录/操作次数聚合，仅统计 `success` 记录的健康度指标。
_Avoid_: 登录次数统计（易被误解为含失败）

### 安全与审计

**审计记录（AuditLog）**:
敏感操作尝试的不可篡改留痕，**含成功与失败**（「谁在什么时候试图做什么」与「做成了什么」同等重要）。
_Avoid_: 操作日志（无成败语义）

**可疑 IP（Suspicious IP）**:
非白名单端点流量 1 分钟内超过阈值的来源地址——仅告警记录，不阻断；限流白名单端点（heartbeat/online/health）不计入。
_Avoid_: 黑名单 IP（本产品不做阻断）
